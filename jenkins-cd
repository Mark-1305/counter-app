pipeline {
    agent any 
    stages{
        stage('git checkout'){
            steps{
                git branch: 'main', url: "https://github.com/Mark-1305/counter-app.git"
            }
        }
    }
}
